# ==============================================
# Full-Stack E-commerce Environment Variables
# ==============================================

# MongoDB Configuration
# ----------------------
# Option 1: Use local MongoDB (via Docker)
MONGODB_URI=mongodb://admin:password@mongodb:27017/egyptfishar?authSource=admin
MONGO_USERNAME=admin
MONGO_PASSWORD=password
MONGO_DATABASE=egyptfishar

# Option 2: Use MongoDB Atlas (Cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/egyptfishar?retryWrites=true&w=majority

# Backend Configuration
# ---------------------
NODE_ENV=production
PORT=5000

# JWT Configuration
# -----------------
# IMPORTANT: Change this to a strong random string in production!
# Generate one with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production-use-openssl-rand
JWT_EXPIRE=7h

# CORS Configuration
# ------------------
# Add your frontend domains (comma-separated)
ALLOWED_ORIGINS=http://localhost:4000,http://localhost:80,http://localhost,http://egypt-fisher.com,https://egypt-fisher.com

# Frontend Configuration
# ----------------------
API_URL=http://ecommerce-backend:5000
