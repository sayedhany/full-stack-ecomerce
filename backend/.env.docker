# Docker Environment Variables
NODE_ENV=development
PORT=5000

# MongoDB Connection
MONGODB_URI=mongodb+srv://sayed591999:sayed591999@cluster0.dwfjqjn.mongodb.net/egyptfishar?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration
JWT_SECRET=your-super-secure-jwt-secret-for-docker-2024
JWT_EXPIRE=7d

# CORS Configuration (for development - allows all origins)
# For production, set specific origins:
# ALLOWED_ORIGINS=http://localhost:3000,http://yourdomain.com

# Allow all origins in development/testing (use with caution)
ALLOW_ALL_ORIGINS=true